input {
  beats {
    port => "5044"
  }
}

filter {
  grok {
    match => { "message" => "%{COMBINEDAPACHELOG}"}
  }

  if "_grokparsefailure" in [tags] {
    drop { }
  }
}

output {
  stdout {
    codec => rubydebug
  }
}
