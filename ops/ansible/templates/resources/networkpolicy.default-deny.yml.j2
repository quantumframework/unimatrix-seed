---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: "{{ ns.metadata.name }}"
  labels:
    app.kubernetes.io/name: default-deny
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
